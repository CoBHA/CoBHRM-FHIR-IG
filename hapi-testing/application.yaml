server:
  port: 8080

spring:
  main:
    allow-circular-references: true

hapi:
  fhir:
    fhir_version: R4
    ig_runtime_upload_enabled: false
    implementationguides:
      cobhrm_current:
        packageUrl: file:///data/hapi/ig-output/package.tgz
        name: us.co.bha-ig
        version: ${HAPI_IG_VERSION:0.3.0-current}
        reloadExisting: true
        installMode: STORE_AND_INSTALL
